# Epic 3 Retrospective: Business Intelligence Dashboard

**Date:** 2026-03-03
**Facilitator:** Bob (Scrum Master)
**Participants:** Gerhard (Project Lead), John (PM), Mary (Analyst), Winston (Architect), Amelia (Dev), Quinn (QA)

---

## Delivery Metrics

- **Stories:** 8/8 completed (100%)
- **Tests:** 377 → 540+ (163+ new tests across 7 stories)
- **Code Review Issues:** ~46 total (~21 HIGH, ~21 MEDIUM, ~4 LOW) — all HIGH + MEDIUM fixed
- **Production Incidents:** 0
- **Regressions:** 0

---

## What Went Well

### 1. Fast Implementation Velocity
- 8 stories covering full BI dashboard delivered rapidly
- Early pattern establishment (Stories 3.1-3.2) created templates that accelerated all subsequent stories
- Decisive architecture choices (recharts, jsPDF client-side, single hooks file) eliminated mid-epic debates

### 2. Component Reuse Pattern
- `MetricsCard` from 3.3 reused in 3.6 without modification
- `DASHBOARD_QUERY_OPTIONS` constant shared across all hooks
- `useDailyMetricsSeries` with column allow-list reused across multiple stories
- `escapeCSVField` pattern established in 3.4, reused in 3.7

### 3. Code Review as Primary Quality Gate
- 46 issues caught across 7 reviews — zero escaped to production
- Security catches: CSV injection (3.4 H1), SQL column injection (3.3 H3), SECURITY DEFINER callable by anon (3.1 H1)
- Consistent 3H + 3-4M pattern per story — thorough and reliable

### 4. Test Coverage Growth
- 163+ tests added across the epic
- Every story shipped with comprehensive test coverage
- Progression: 377 → 399 → 445 → 480 → 497 → 522 → 540

### 5. Zero Production Incidents
- Clean epic delivery — no regressions, no hotfixes, no rollbacks

---

## What Didn't Go Well

### 1. Recurring Code Review Patterns (Not Learning from Prior Fixes)
- **Responsive breakpoint inversion** (`p-12 md:p-6` → `p-6 md:p-12`): Hit in both 3.2 and 3.3
- **Supabase client at module level**: Flagged as HIGH in 3.2, same anti-pattern reappeared
- **`undefined` vs `null` semantics**: Two issues in 3.6 from conflating disabled-query state with no-data state

### 2. Architecture Spec Drift
- Epic title says "Chart.js" — project uses recharts v2.15.0 (every story rediscovered this)
- Architecture doc specified object-format query keys — team kept flat format (correct decision, but spec never updated)
- `refetchInterval` 30s (Story 3.2 AC) vs 60s (architecture doc) — unresolved until Story 3.8

### 3. Epic 2 Action Items — 0/5 Completed
| Action Item | Owner | Status |
|---|---|---|
| Auto-generate Supabase types | Amelia | ⏳ Not done |
| Complete TypeScript test setup docs | Amelia | ⏳ Not done |
| Write tests for audit UI components | Dana | ❌ Carried 3 epics |
| Update stale connector_config seed data | Charlie | ⏳ Not done |
| Create n8n monitoring workflow | Charlie | ⏳ Not done |

### 4. CRITICAL: Dashboard Has No Live Data
- Dashboard UI is fully built but displays **empty charts** for real users
- `delivery_attempts` table never populated — `calculate_daily_metrics` cron has no input data
- No planned connection between Epic 2 data ingestion and Epic 3 dashboard metrics
- Beetrack integration missing — cannot retrieve delivery status or failure reasons
- **User onboarding is blocked** — the MVP core product is non-functional end-to-end

---

## Critical Finding: Data Pipeline Gap

```
Epic 2 (Data IN)          MISSING GLUE           Epic 3 (Display)
─────────────────    ─────────────────────    ─────────────────
Easy CSV ✅          orders → delivery_       Dashboard UI ✅
DispatchTrack ✅       attempts ❌            Charts ✅
Beetrack ❌          delivery_attempts →      Export ✅
                       performance_metrics ❌  Caching ✅
                     calculate_daily_metrics
                       cron runs but has
                       no input data ❌
```

**Impact:** PRD success metric "First customer using BI dashboard daily" cannot be achieved. Dashboard exists but is empty.

---

## Action Items

### CRITICAL (Must complete before Epic 4)

1. **Beetrack Integration** — Build connector to retrieve delivery status updates and failure reasons from Beetrack API. Populate `orders` status and `delivery_attempts` table.
   - Owner: TBD (Course Correction will scope)

2. **Data Pipeline: Orders → Metrics** — Connect order delivery outcomes to `delivery_attempts` table so `calculate_daily_metrics` cron produces real data for the dashboard.
   - Owner: TBD (Course Correction will scope)

3. **End-to-End Validation** — Verify complete flow: raw order ingestion → delivery attempt recording → nightly metrics calculation → dashboard displays correct data.
   - Owner: TBD (Course Correction will scope)

### LOW PRIORITY (Cleanup)

4. **Update epics.md** — Change "Chart.js" references to "recharts" to prevent future confusion.
   - Owner: Anyone

5. **Audit UI tests** — Carried from Epic 1. Either do them or explicitly decide to drop the action item.
   - Owner: Team decision

---

## Decision: Defer Epic 4

**Epic 4 (Pickup Verification Mobile PWA) is deferred** until the dashboard is live with real data and a user is successfully onboarded.

**Next step:** Run Course Correction [CC] to properly scope the Beetrack integration and data pipeline work.

---

## Team Agreements

1. Retrospective action items that aren't completed within the next epic should be explicitly dropped or re-prioritized — no more silent carry-forward
2. Continue the code review process — 46 catches with 0 production incidents proves its value
3. Early pattern establishment (Stories 1-2 of an epic) is a velocity multiplier — invest time there

---

*Generated: 2026-03-03*
