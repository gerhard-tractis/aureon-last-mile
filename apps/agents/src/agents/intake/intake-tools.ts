// src/agents/intake/intake-tools.ts — Legacy file, kept for import compatibility
// The INTAKE agent now uses direct orchestration (processIntakeSubmission)
// rather than the tool-calling loop pattern.
export {};
