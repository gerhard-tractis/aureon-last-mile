{
  "name": "@aureon/worker",
  "version": "0.1.0",
  "private": true,
  "description": "Aureon automation worker — VPS runtime for data ingestion connectors",
  "engines": {
    "node": ">=20"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest --watch"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "@types/node-cron": "^3.0.11",
    "@types/pg": "^8.16.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "@sentry/node": "^9.20.0",
    "node-cron": "^4.2.1",
    "pg": "^8.18.0"
  }
}
